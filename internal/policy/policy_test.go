package policy_test

// tp "tee-node/client/policy"

// ! Count valid signatures

// ? Test decoding a policy with a wrong format (should fail)

// ? Test sending two signatures for the same public key

// ? Test sending a signature for a public key that is not a voter in the previous policy (shouldnt count)

// ? Test sending a signature with a wrong signature (shouldnt count)

// ? Test sending a signature for a different policy Hash (shouldnt count)

// ? test if voter weight is incremented properly

// ! Test encoding decoding of the policy

// ! VerifyPolicyFreshness

// ? some basic tests

// ? some basic tests
